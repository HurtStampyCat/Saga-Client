appId: 'Saga'
productName: 'Saga'
linux:
  artifactName: Saga_Setup_Linux_${arch}.${ext}
  target:
    - target: appImage
      arch:
        - x64
  category: network
mac:
  artifactName: Saga_Setup_macOS_${arch}.${ext}
  target:
    - target: dmg
      arch:
        - x64
nsis:
  artifactName: Saga_Setup_Windows_${arch}.${ext}
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true